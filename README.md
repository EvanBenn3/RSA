# RSA
An RSA implementation that was created to succeed the AES project.

It is in deep WIP. The project originally set out to implement arbitrary precision from scratch but has since been abandoned to instead use a working library.
